// utils.h
#ifndef UTILS_H
#define UTILS_H

// Clears the terminal screen
void clear();

// Prints a horizontal separator line
void separator();

// Prints colored text using ANSI color codes
void print_colored(const char *text, const char *color_code);

#endif
